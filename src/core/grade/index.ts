/**
 * Colour grading. `looks.ts` is the vocabulary (what a grade is, the sliders, the seven looks);
 * `maths.ts` turns a grade into the numbers the preview and the export both use.
 */
export * from './looks';
export * from './maths';
